//import { Directive } from "vue";

export const wordTooltip = {
    created: (el, binding) => { 



        // console.log(binding,el)
        // /**
        //  * Add event listeners, that may be all
        //  */

        // el.addEventListener('onmouseenter', () => {
        //     //activeWord.value = binding.value;
        //     console.log(binding.value)
        // });

        // el.addEventListener('onmouseout', () => {
        //     //activeWord.value = '0';
        // });

    }
}